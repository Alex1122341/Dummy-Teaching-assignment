'use strict';
(()=>{const {auth,db}=UCVM.init(),$=id=>document.getElementById(id),e=UCVM.esc;let me,groups=[],sessions=[];
 $('signout').onclick=()=>auth.signOut();$('history-tab').onclick=()=>{$('sessions-panel').hidden=true;$('history-panel').hidden=false;UCVM.logs($('history-panel'))};$('sessions-tab').onclick=()=>{$('sessions-panel').hidden=false;$('history-panel').hidden=true};
 function render(){const mine=sessions.filter(s=>UCVM.admin(me)||(s.assignments||[]).some(a=>String(a.ucid||'')===String(me.facultyId||''))||groups.some(g=>(g.courseIds||[]).includes(String(s.course))));$('sessions-body').innerHTML=mine.sort((a,b)=>`${a.date}${a.start}`.localeCompare(`${b.date}${b.start}`)).map(s=>`<tr><td>${e(s.date)}<small>${e(s.start)}–${e(s.end)}</small></td><td>${e(s.course)} · ${e(s.topic)}</td><td>${e((s.assignments||[]).map(a=>a.name).join('; ')||s.instructor)}</td><td>${UCVM.admin(me)?'<a href="index.html">Edit in timetable</a>':['hicc','visc'].includes(me.role)?'View only in Spark Basic Mode':'View only'}</td></tr>`).join('')||'<tr><td colspan="4">No assigned sessions or group courses yet.</td></tr>'}
 async function loadGroups(){if(UCVM.admin(me)){groups=[];return}if(!['hicc','visc'].includes(me.role)){groups=[];return}const s=await db.collection('faculty_groups').where('memberUids','array-contains',auth.currentUser.uid).get();groups=s.docs.map(d=>({id:d.id,...d.data()}));}
 auth.onAuthStateChanged(async u=>{if(!u){location.replace('index.html');return}try{me=(await db.doc(`users/${u.uid}`).get()).data();if(!await UCVM.ready(u,me))return;me={...me,role:UCVM.role(me.role)};UCVM.watch(u,(await db.doc(`users/${u.uid}`).get()).data());$('identity').textContent=`${me.name||u.email} · ${UCVM.label(me.role)}`;$('management-link').hidden=!(UCVM.general(me)||me.role==='hicc');$('directory-link').hidden=!UCVM.admin(me);await loadGroups();$('session-note').textContent=['hicc','visc'].includes(me.role)?'Spark Basic Mode: view sessions for your HICC groups. Instructor replacement remains ADFA-only until server Functions are enabled.':'Your assigned sessions and the faculty change history.';$('content').hidden=false;db.collection('sessions').onSnapshot(s=>{sessions=s.docs.map(d=>({id:d.id,...d.data()}));render()},err=>$('status').textContent=err.message)}catch(err){$('status').textContent=err.message}});
})();
